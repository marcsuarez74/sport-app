# Sport App Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** App mobile PWA (Vite + React + TS) qui lit un .md hebdo structuré (importé sur le téléphone) et affiche Cuisine (courses/menu/batch) + profils Marc & Mélanie, avec coches et suivi poids persistés en local.

**Architecture:** 100% front. Un .md par semaine → parser → modèle typé → vues. État = localStorage (semaine courante, coches par semaine, poids par profil). PWA offline-first, déployée sur GitHub Pages.

**Tech Stack:** Vite, React 18, TypeScript, js-yaml (frontmatter), vitest + Testing Library (happy-dom), vite-plugin-pwa.

**Design constraints (user):** dark mode ONLY, cohérent, fluide (transitions douces), ultra visible (contraste élevé, cibles tactiles ≥ 48 px, hiérarchie forte).

---

## Structure de fichiers

```
sport-app/
├── index.html
├── package.json / vite.config.ts / tsconfig*.json
├── .github/workflows/deploy.yml
├── public/icons/                      (générées via @vite-pwa/assets-generator)
├── src/
│   ├── main.tsx / App.tsx / styles.css
│   ├── assets/semaine-exemple.md      (Sem 1, Menu A — contenu réel)
│   ├── lib/
│   │   ├── model.ts                   types du domaine
│   │   ├── parse.ts                   .md → WeeklyData (+ warnings)
│   │   ├── storage.ts                 localStorage: semaine, coches, poids
│   │   └── dates.ts                   jour courant (fr)
│   └── components/
│       ├── ImportButton.tsx  ImportScreen.tsx  WeekBanner.tsx  TabBar.tsx
│       ├── Checklist.tsx     Sparkline.tsx
│       ├── cuisine/ShoppingList.tsx  cuisine/MenuView.tsx  cuisine/BatchView.tsx
│       └── ProfileView.tsx
└── tests/ (miroir de src/, vitest)
```

---

### Task 1 : Scaffold

**Files:** `sport-app/*` (nouveau repo git, déjà créé avec docs/)

- [ ] `npm create vite@latest . -- --template react-ts` dans `sport-app/`, puis commit initial
- [ ] `npm i js-yaml && npm i -D vitest @testing-library/react @testing-library/user-event @testing-library/jest-dom happy-dom vite-plugin-pwa @types/js-yaml`
- [ ] Nettoyer le template (logo, css démo), `.gitignore` standard Vite
- [ ] Vitest : `test: { environment: 'happy-dom', globals: true, setupFiles: './tests/setup.ts' }` ; `tests/setup.ts` = `import '@testing-library/jest-dom'`
- [ ] Vérifier : `npm run build && npm test` passe → commit

### Task 2 : Modèle + parser (TDD — cœur critique)

**Files:** `src/lib/model.ts`, `src/lib/parse.ts`, `tests/parse.test.ts`

- [ ] **Types** (`model.ts`) :

```ts
export type ProfileKey = 'marc' | 'melanie';
export interface WeekMeta { semaine: string; menu: string; du: string; au: string; titre?: string }
export interface CourseItem { id: string; rayon: string; label: string }
export interface MenuDay { jour: string; dejeunerMarc?: string; dejeunerMelanie?: string;
  dinerFamille?: string; dinerMelanie?: string; batch?: string }
export interface ChecklistItem { id: string; label: string }
export interface ProfileData { cibles: string[]; seances: ChecklistItem[]; rappels: string[] }
export interface WeeklyData { meta: WeekMeta; courses: CourseItem[]; menu: MenuDay[];
  batch: ChecklistItem[]; profiles: Record<ProfileKey, ProfileData> }
export interface ImportedWeek { raw: string; data: WeeklyData; importedAt: string }
```

- [ ] **Tests d'abord** (`tests/parse.test.ts`) — cas couverts :
  1. Parse complet d'une semaine valide → meta, courses groupées par rayon avec ids stables, 7 jours de menu avec les 5 champs mappés, batch cochable, profils marc/melanie (cibles, séances, rappels)
  2. `- label` sans case → item non coché ; `- [x] label` → coché
  3. Section `## Mélanie` (accent) → profil `melanie` (slugify sans accents)
  4. Erreurs : sans frontmatter → throw `Frontmatter introuvable` ; champ `semaine` manquant → throw ; body vide → sections vides + warnings
  5. Ligne menu à clé inconnue (`- dessert: tarte`) → ignorée + warning listé
  6. IDs : même label → même id (stabilité des coches entre imports)

```ts
// src/lib/parse.ts (implémentation de référence)
import { load } from 'js-yaml';
import type { ChecklistItem, CourseItem, MenuDay, ProfileData, WeekMeta, WeeklyData } from './model';

export function slugify(s: string): string {
  return s.normalize('NFD').replace(/[\u0300-\u036f]/g, '').toLowerCase()
    .replace(/[^a-z0-9]+/g, '-').replace(/(^-|-$)/g, '');
}
const MENU_KEYS: Record<string, keyof MenuDay> = {
  'dejeuner-marc': 'dejeunerMarc', 'dejeuner-melanie': 'dejeunerMelanie',
  'diner-famille': 'dinerFamille', 'diner-melanie': 'dinerMelanie', 'batch': 'batch',
};

export interface ParseResult { data: WeeklyData; warnings: string[] }

export function parseWeeklyFile(raw: string): ParseResult {
  const m = raw.match(/^---\r?\n([\s\S]*?)\r?\n---\r?\n?/);
  if (!m) throw new Error('Frontmatter introuvable : le fichier doit commencer par --- (semaine, menu, du, au).');
  const metaDoc = load(m[1]) as Record<string, string | undefined>;
  if (!metaDoc.semaine || !metaDoc.menu || !metaDoc.du || !metaDoc.au)
    throw new Error('Frontmatter incomplet : semaine, menu, du, au sont requis.');
  const meta: WeekMeta = { ...metaDoc } as WeekMeta;

  const warnings: string[] = [];
  const sections = splitH2(raw);
  const courses = parseCourses(sections.get('courses') ?? '');
  const menu = parseMenu(sections.get('menu') ?? '', warnings);
  const batch = parseItems(sections.get('batch') ?? '', 'batch');
  const profiles = {
    marc: parseProfile(sections.get('marc') ?? ''),
    melanie: parseProfile(sections.get('melanie') ?? ''),
  };
  for (const s of ['courses', 'menu', 'batch', 'marc', 'melanie'])
    if (!sections.has(s)) warnings.push(`Section ## « ${s} » absente ou vide.`);
  return { data: { meta, courses, menu, batch, profiles }, warnings };
}

function splitH2(body: string): Map<string, string> {
  const map = new Map<string, string>();
  let cur = '', buf: string[] = [];
  for (const line of body.split(/\r?\n/)) {
    const h = line.match(/^##\s+(.+?)\s*$/);
    if (h) { if (cur) map.set(cur, buf.join('\n')); cur = slugify(h[1]); buf = []; }
    else if (cur) buf.push(line);
  }
  if (cur) map.set(cur, buf.join('\n'));
  return map;
}

function parseCourses(text: string): CourseItem[] {
  const out: CourseItem[] = []; let rayon = 'divers';
  for (const line of text.split(/\r?\n/)) {
    const h = line.match(/^###\s+(.+?)\s*$/);
    if (h) { rayon = slugify(h[1]); continue; }
    const it = line.match(/^[-*]\s+(?!\[)(.+?)\s*$/);
    if (it) out.push({ id: `courses:${rayon}:${slugify(it[1])}`, rayon, label: it[1] });
  }
  return out;
}

function parseMenu(text: string, warnings: string[]): MenuDay[] {
  const days: MenuDay[] = []; let day: MenuDay | null = null;
  for (const line of text.split(/\r?\n/)) {
    const h = line.match(/^###\s+(.+?)\s*$/);
    if (h) { if (day) days.push(day); day = { jour: h[1] }; continue; }
    const kv = line.match(/^[-*]\s+([a-z-]+)\s*:\s*(.+?)\s*$/);
    if (kv && day) {
      const key = MENU_KEYS[kv[1]];
      if (key) (day as Record<string, string | undefined>)[key] = kv[2];
      else warnings.push(`Clé menu inconnue « ${kv[1]} » ignorée (${day.jour}).`);
    }
  }
  if (day) days.push(day);
  return days;
}

function parseItems(text: string, section: string): ChecklistItem[] {
  const out: ChecklistItem[] = [];
  for (const line of text.split(/\r?\n/)) {
    const plain = line.match(/^[-*]\s+(.+?)\s*$/); if (!plain) continue;
    const withBox = plain[1].match(/^\[( |x|X)\]\s+(.+)$/);
    out.push({ id: `${section}:${slugify(withBox ? withBox[2] : plain[1])}`, label: withBox ? withBox[2] : plain[1] });
  }
  return out;
}

function parseProfile(text: string): ProfileData {
  const res: ProfileData = { cibles: [], seances: [], rappels: [] };
  let cur: 'cibles' | 'seances' | 'rappels' | null = null;
  for (const line of text.split(/\r?\n/)) {
    const h = line.match(/^###\s+(.+?)\s*$/);
    if (h) { const k = slugify(h[1]); cur = k === 'cibles' || k === 'seances' || k === 'rappels' ? k : null; continue; }
    const plain = line.match(/^[-*]\s+(.+?)\s*$/); if (!plain || !cur) continue;
    if (cur === 'cibles' || cur === 'rappels') res[cur].push(plain[1]);
    else {
      const withBox = plain[1].match(/^\[( |x|X)\]\s+(.+)$/);
      res.seances.push({ id: `seances:${slugify(withBox ? withBox[2] : plain[1])}`, label: withBox ? withBox[2] : plain[1] });
    }
  }
  return res;
}
```

- [ ] Fixtures de test = extraits exacts du format (cf. Task 9) ; valider `slugify('Mélanie') === 'melanie'`, `slugify('Poulet 600 g') === 'poulet-600-g'`
- [ ] `npm test` → rouge puis vert → commit `feat: weekly md parser`

### Task 3 : Storage + dates (TDD)

**Files:** `src/lib/storage.ts`, `src/lib/dates.ts`, `tests/storage.test.ts`

```ts
// src/lib/storage.ts
import type { ImportedWeek, ProfileKey, WeeklyData } from './model';
const WEEK_KEY = 'sportapp:week';
const checksKey = (s: string) => `sportapp:checks:${s}`;
const weightsKey = (p: string) => `sportapp:weights:${p}`;
export interface WeightEntry { date: string; kg: number }

export const saveWeek = (raw: string, data: WeeklyData): void =>
  localStorage.setItem(WEEK_KEY, JSON.stringify({ raw, data, importedAt: new Date().toISOString() } satisfies ImportedWeek));
export const loadWeek = (): ImportedWeek | null => {
  const s = localStorage.getItem(WEEK_KEY); return s ? JSON.parse(s) as ImportedWeek : null;
};
export const getChecks = (semaine: string): Record<string, boolean> =>
  JSON.parse(localStorage.getItem(checksKey(semaine)) ?? '{}');
export const setCheck = (semaine: string, id: string, done: boolean): void => {
  const c = getChecks(semaine); c[id] = done;
  localStorage.setItem(checksKey(semaine), JSON.stringify(c));
};
export const getWeights = (p: ProfileKey): WeightEntry[] =>
  JSON.parse(localStorage.getItem(weightsKey(p)) ?? '[]');
export const addWeight = (p: ProfileKey, date: string, kg: number): WeightEntry[] => {
  const list = getWeights(p).filter(w => w.date !== date).concat({ date, kg })
    .sort((a, b) => a.date.localeCompare(b.date));
  localStorage.setItem(weightsKey(p), JSON.stringify(list)); return list;
};
```

```ts
// src/lib/dates.ts
export const JOURS = ['lundi','mardi','mercredi','jeudi','vendredi','samedi','dimanche'] as const;
export const todayKey = (): (typeof JOURS)[number] => JOURS[(new Date().getDay() + 6) % 7];
```

- [ ] Tests : save/load roundtrip, coches isolées par semaine (S39 vs S40), addWeight écrase même date + trie, todayKey mocké (vi.setSystemTime) → vert → commit

### Task 4 : Sparkline + Checklist (TDD)

**Files:** `src/components/Sparkline.tsx`, `src/components/Checklist.tsx`, tests

```tsx
// Sparkline.tsx — SVG pur, zéro dépendance
export function Sparkline({ values, color = '#4f6bed', height = 48 }: { values: number[]; color?: string; height?: number }) {
  if (values.length < 2) return <p className="muted">Ajoutez au moins 2 pesées.</p>;
  const min = Math.min(...values), max = Math.max(...values);
  const pts = values.map((v, i) => {
    const x = (i / (values.length - 1)) * 100;
    const y = 100 - ((v - min) / (max - min || 1)) * 100;
    return `${x},${y}`;
  }).join(' ');
  return (
    <svg viewBox="0 0 100 100" preserveAspectRatio="none" style={{ width: '100%', height }} role="img" aria-label="évolution du poids">
      <polyline points={pts} fill="none" stroke={color} strokeWidth="2" vectorEffect="non-scaling-stroke" />
    </svg>
  );
}
```

```tsx
// Checklist.tsx — coches persistées par semaine
import { useState } from 'react';
import type { ChecklistItem } from '../lib/model';
import { getChecks, setCheck } from '../lib/storage';

export function Checklist({ items, semaine }: { items: ChecklistItem[]; semaine: string }) {
  const [checks, setChecks] = useState<Record<string, boolean>>(() => getChecks(semaine));
  const toggle = (item: ChecklistItem) => {
    const done = !checks[item.id];
    setCheck(semaine, item.id, done);
    setChecks(c => ({ ...c, [item.id]: done }));
  };
  return (
    <ul className="checklist">
      {items.map(it => (
        <li key={it.id}>
          <label className={checks[it.id] ? 'done' : ''}>
            <input type="checkbox" checked={!!checks[it.id]} onChange={() => toggle(it)} />
            <span>{it.label}</span>
          </label>
        </li>
      ))}
    </ul>
  );
}
```

- [ ] Tests : clic → localStorage mis à jour (getChecks reflète), re-render conserve l'état, classe `done` appliquée ; Sparkline : < 2 valeurs → message, sinon polyline rendue → commit

### Task 5 : Vue Cuisine — Courses (TDD)

**Files:** `src/components/cuisine/ShoppingList.tsx`, test

- Groupe par `rayon` (ordre de première apparition), entête rayon, `<Checklist>` par groupe, barre de progression `cochés/total`.
- Test : items de 2 rayons → 2 groupes ; progress affiche `3/8` après 3 coches.

### Task 6 : Vue Cuisine — Menu (TDD)

**Files:** `src/components/cuisine/MenuView.tsx`, test

- Une carte par `MenuDay` : titre = jour, lignes libellées FR : `Déjeuner Marc` / `Déjeuner Mélanie` / `Dîner famille` / `Assiette keto Mé` / `Batch du jour` (masqué si absent).
- Jour courant (`todayKey()`) : carte avec classe `today` + badge « Aujourd'hui ».
- Test : carte du jour mocké (vi.setSystemTime sur un mardi) a la classe `today`.

### Task 7 : Batch + ProfileView (TDD)

**Files:** `src/components/cuisine/BatchView.tsx`, `src/components/ProfileView.tsx`, tests

- `BatchView` : `<Checklist items={batch} semaine>` + bandeau « Gros batch : dimanche, 45-60 min ».
- `ProfileView({ profileKey, data, semaine })` :
  - `Cibles` : `<ul>` des lignes
  - `Séances` : `<Checklist items={data.seances} semaine>`
  - `Suivi poids` : formulaire (date défaut aujourd'hui + number step 0.1 + bouton « Ajouter ») → `addWeight` → historique + `<Sparkline values={...kg}>`, couleur `#e07b39` marc / `#3d9a6c` melanie
  - `Rappels` : `<ul>`
- Tests : ajout d'une pesée → apparaît dans la liste ; séance cochée persistée.

### Task 8 : App shell — onglets, import, bannière (TDD)

**Files:** `src/App.tsx`, `src/components/ImportButton.tsx`, `src/components/ImportScreen.tsx`, `src/components/WeekBanner.tsx`, `src/components/TabBar.tsx`, `src/main.tsx`, tests

```tsx
// ImportButton.tsx
import { useState } from 'react';
import { parseWeeklyFile } from '../lib/parse';
import { loadWeek, saveWeek } from '../lib/storage';

export function ImportButton({ onImported, label = 'Importer un .md' }: { onImported: () => void; label?: string }) {
  const [error, setError] = useState('');
  const onChange = async (e: React.ChangeEvent<HTMLInputElement>) => {
    const file = e.target.files?.[0]; if (!file) return;
    try {
      const raw = await file.text();
      const { data, warnings } = parseWeeklyFile(raw);
      const current = loadWeek();
      if (current && current.data.meta.semaine !== data.meta.semaine &&
          !window.confirm(`Remplacer « ${current.data.meta.semaine} » par « ${data.meta.semaine} » ?`)) return;
      saveWeek(raw, data);
      if (warnings.length) console.warn('Import:', warnings);
      onImported();
    } catch (err) { setError(err instanceof Error ? err.message : 'Fichier invalide'); }
  };
  return (
    <div className="import">
      <label className="btn">{label}<input type="file" accept=".md,text/markdown" hidden onChange={onChange} /></label>
      {error && <p className="error" role="alert">{error}</p>}
    </div>
  );
}
```

- `App.tsx` : state `week = loadWeek()` + `refresh()` ; si `null` → `ImportScreen` (pitch + `ImportButton` + bouton « Charger la semaine d'exemple » qui importe `import sample from './assets/semaine-exemple.md?raw'`) ; sinon : `WeekBanner` (semaine · menu · du→au formatés fr) + `TabBar` (Cuisine / Marc / Mélanie, state local) + vues.
- Tests : ImportScreen affiché sans données ; simulation import (user.upload d'un File avec fixture) → onglet Cuisine rendu ; erreur de parse → `role="alert"`.

### Task 9 : Semaine d'exemple (contenu réel)

**Files:** `src/assets/semaine-exemple.md` — généré depuis `tableau-repas-jour-par-jour-2026-09-02.md` (Menu A, Sem 1) et `plan-diet.md` (contenu complet spécifié dans la conversation, avec frontmatter `semaine: 2026-S39, menu: A, du: 2026-09-21, au: 2026-09-27`, sections Courses par rayon, Menu 7 jours × 5 clés, Batch checklist, Marc et Melanie avec Cibles/Séances/Rappels).

- [ ] Test d'intégration : `parseWeeklyFile(sample)` → 7 jours, courses > 15 items, 2 profils, warnings vides → commit

### Task 10 : PWA + styles (dark mode only, ultra visible)

**Files:** `vite.config.ts`, `public/`, `src/styles.css`, `index.html`

- [ ] `vite-plugin-pwa` : `registerType: 'autoUpdate'`, manifest (`name: 'Sport App'`, `short_name: 'Sport'`, `display: 'standalone'`, `theme_color: '#0f1115'`, `background_color: '#0f1115'`), `workbox.globPatterns: ['**/*.{js,css,html,svg,png,woff2}']` ; `main.tsx` → `import 'virtual:pwa-register'`
- [ ] Icônes : `npm i -D @vite-pwa/assets-generator` puis génération depuis un SVG source simple (« 🏋️ » sur fond indigo) → commit des PNG
- [ ] `styles.css` — design system dark only :
  - Variables : `--bg: #0f1115` · `--surface: #1a1d27` · `--surface-2: #232735` · `--text: #f2f4f8` · `--muted: #9aa3b5` · `--accent-cuisine: #5c6bc0` · `--accent-marc: #ff9e57` · `--accent-melanie: #4ade80`
  - Contraste élevé (texte ≥ 4.5:1), gros titres, cartes avec radius 16px et bordures subtiles
  - Transitions fluides (`transition: 0.2s ease` sur cartes/boutons), active states (scale 0.97)
  - TabBar fixe en bas, 3 onglets, safe-area iOS (`env(safe-area-inset-bottom)`), icônes emoji + labels
  - Checkboxes ≥ 48 px, `.done` avec texte barré + atténué, barre de progression visible
  - `.today` : bordure accent + badge lumineux
- [ ] `index.html` : `lang="fr"`, viewport, theme-color, `<meta name="apple-mobile-web-app-capable">`
- [ ] `npm run build && npm run preview` → installable (DevTools > Application) → commit

### Task 11 : Déploiement GitHub Pages

**Files:** `.github/workflows/deploy.yml`, `README.md`

```yaml
name: Deploy
on: { push: { branches: [main] }, workflow_dispatch: {} }
permissions: { contents: read, pages: write, id-token: write }
concurrency: { group: pages, cancel-in-progress: true }
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: npm }
      - run: npm ci
      - run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with: { path: dist }
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment: { name: github-pages, url: ${{ steps.deployment.outputs.page_url }} }
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
```

- [ ] `vite.config.ts` → `base: '/sport-app/'`
- [ ] README : format du .md hebdo (référence à l'exemple), import sur téléphone, commandes dev/build, déploiement (Settings > Pages > GitHub Actions)
- [ ] Commit + push → `https://<user>.github.io/sport-app/`

### Task 12 : Vérification finale

- [ ] `npm run lint && npx tsc -b && npm test && npm run build` — tout vert
- [ ] Test manuel (preview) : import, coches, pesée, onglets, offline, responsive
- [ ] Revue de code globale (subagent) → ajustements → commit

---

## Auto-revue

- Couverture spec : courses ✓ menu ✓ batch ✓ profils ✓ import md ✓ coches ✓ poids ✓ PWA offline ✓ Pages ✓ semaine dans le .md ✓ dark mode only ✓
- YAGNI : pas d'archives, pas de sync, pas de CSV Garmin
- Risque connu : format .md strict — documenté dans l'exemple embarqué + README, erreurs signalées à l'import sans casser l'ancienne semaine
